{
  "fingerprintId": "rabbitmq-management_any",
  "name": "rabbitmq-management",
  "protocol": "http",
  "tags": ["middleware", "mq", "devops"],
  "info": {
    "version": "any",
    "author": "leo-seed",
    "description": "通过 /api/overview 接口的 JSON 响应识别 RabbitMQ Management 插件。",
    "vulnerabilities": [
      {
        "title": "默认凭证 guest/guest",
        "severity": "high",
        "description": "RabbitMQ 默认账号密码为 guest/guest，若对外开放 Management 插件且未修改默认凭证，攻击者可完全控制消息队列，并可利用 Shovel 插件发起 SSRF 探测内网。"
      }
    ]
  },
  "rule": {
    "requests": [
      {
        "method": "GET",
        "uri": "/api/overview",
        "timeout": 4000,
        "charset": "UTF-8",
        "maxBodyBytes": 65536
      }
    ],
    "script": "var b = String(body || ''); b.indexOf('rabbitmq_version') >= 0 || b.indexOf('erlang_version') >= 0"
  }
}
